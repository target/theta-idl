language-version: 1.0.0
avro-version: 1.0.0
---

/* This module imports every Theta module we use in the Python test
 * suite. This lets us generate every single Python class we need with
 * a single command:
 *
 * theta-python --path ./python-tests/modules --module python_tests --target $out
 *
 */

import importing

import variant

import newtype

import shadowing
