language-version: 1.0.0
avro-version: 1.0.0
---

/* This module imports every Theta module we use in the Kotlin test
 * suite. This lets us generate every single Kotlin class we need with
 * a single command:
 *
 * theta --path modules kotlin -m kotlin_tests --target $out
 *
 */

import enum

import importing

import variant

import newtype

import shadowing
